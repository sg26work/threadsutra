import assert from'node:assert/strict';import{chromium}from'playwright';const base=process.env.ERETAIL_BASE_URL||'http://127.0.0.1:3011',browser=await chromium.launch({headless:true});try{const page=await browser.newPage();await page.addInitScript(()=>localStorage.setItem('vin_user',JSON.stringify({username:'responsive-shell-verifier'})));for(const viewport of[{width:1200,height:623},{width:1200,height:480},{width:1920,height:1080}]){await page.setViewportSize(viewport);await page.goto(`${base}/app/dashboard`,{waitUntil:'domcontentloaded'});const m=await page.evaluate(()=>{const box=s=>{const e=document.querySelector(s),r=e.getBoundingClientRect();return{x:r.x,y:r.y,width:r.width,height:r.height,scrollHeight:e.scrollHeight,clientHeight:e.clientHeight}};return{body:document.body.scrollHeight,doc:document.documentElement.scrollHeight,header:box('header'),rail:box('[data-sidebar-rail]'),main:box('main'),footer:box('footer')}});assert.equal(m.body,viewport.height);assert.equal(m.doc,viewport.height);assert.deepEqual({x:m.header.x,y:m.header.y,width:m.header.width,height:m.header.height},{x:0,y:0,width:viewport.width,height:51});assert.deepEqual({x:m.rail.x,y:m.rail.y,width:m.rail.width,height:m.rail.height},{x:0,y:0,width:50,height:viewport.height});assert.equal(m.main.x,50);assert.equal(m.main.y,51);assert.equal(m.main.width,viewport.width-50);assert.equal(m.main.clientHeight,viewport.height-70);assert.equal(m.footer.x,50);assert.equal(m.footer.y,viewport.height-19);assert.equal(m.footer.width,viewport.width-50);assert.equal(m.footer.height,19)}console.log('PASS global responsive shell: viewport-bound header, full-height 50px rail, internally scrolling content, and bottom footer at three desktop sizes.')}finally{await browser.close()}
