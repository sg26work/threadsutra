import assert from'node:assert/strict';import{chromium}from'playwright';const base=process.env.ERETAIL_BASE_URL||'http://127.0.0.1:3002',send=async body=>{const r=await fetch(`${base}/api/inbound-create-edit`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});return{r,j:await r.json()}};const meta=await(await fetch(`${base}/api/inbound-create-edit`)).json();assert.equal(meta.maxImportRows,2000);assert.ok(meta.purchaseOrders.length);let x=await send({action:'po',poNo:''});assert.equal(x.j.error,'PO No is mandatory');x=await send({action:'po',poNo:'PO20003012'});assert.equal(x.j.poNo,'PO20003012');x=await send({action:'import',rows:[]});assert.equal(x.j.error,'Nothing To Import');x=await send({action:'import',rows:[{sku_code:'INB-TEST-SKU',sku_name:'Inbound Test SKU',rcvdUOMQty:2}]});assert.equal(x.j.successItems,1);const lines=x.j.DTOList;x=await send({action:'save',poNo:'',lines});assert.equal(x.j.error,'PO No is mandatory');x=await send({action:'save',poNo:'PO20003012',vendor:'NovaTech Electronics',warehouse:'Bengaluru WH',lines,remarks:'Legacy inbound'});assert.equal(x.r.status,201);assert.equal(x.j.row.status,'Pending Confirmation');const row=x.j.row;x=await send({action:'confirm',...row,id:row.id,poNo:row.poNo,lines:row.lines});assert.equal(x.j.row.status,'Confirmed');const loaded=await(await fetch(`${base}/api/inbound-create-edit?inboundNo=${encodeURIComponent(row.inboundNo)}`)).json();assert.equal(loaded.inboundNo,row.inboundNo);
const b=await chromium.launch({headless:true}),p=await b.newPage({viewport:{width:1440,height:1000}}),errors=[];p.on('pageerror',e=>errors.push(e.message));p.on('console',m=>m.type()==='error'&&errors.push(m.text()));try{await p.goto(base);const cap=(await p.locator('.font-mono').textContent()).trim();await p.getByPlaceholder('Username').fill('legacy-inbound');await p.getByPlaceholder('Password').fill('local');await p.getByPlaceholder('Enter captcha').fill(cap);await p.getByRole('button',{name:'Login'}).click();await p.waitForURL('**/app/dashboard');await p.goto(`${base}/app/r/inbound-create-edit`);await p.getByText(/screen will be deprecated on 25th Nov 2025/).waitFor();for(const a of['Save Document','Add New','Save','Confirm','Import','Add New Row','Delete Row'])await p.getByRole('button',{name:a,exact:true}).waitFor();await p.getByLabel('PO No',{exact:true}).waitFor();await p.getByRole('button',{name:'Save',exact:true}).click();await p.getByText('PO No is mandatory').waitFor();await p.getByRole('button',{name:'Add New Row',exact:true}).click();for(const h of['SKU Code','SKU Name','PO Qty','Rcvd UOM Qty','Rcvd Qty','Invoice Qty','Bin Location','LPN','Unit Price','Line Amount','MRP','Expiry Date','Mfg date','Lottable04','Batch No','Lottable06'])await p.getByRole('columnheader',{name:h,exact:true}).waitFor();await p.getByRole('button',{name:'Import',exact:true}).click();await p.getByLabel('Import rows').waitFor();await p.getByRole('button',{name:'Reset',exact:true}).last().click();assert.equal(await p.locator('vite-error-overlay').count(),0);assert.deepEqual(errors.filter(e=>!e.includes('status of 400')),[]);console.log('PASS Inbound Create/Edit: deprecation notice, PO workflow, line grid, save/confirm lifecycle, import results, documents, reset, and clean browser state.')}finally{await b.close()}
