import {chromium}from'playwright';const base=process.env.VENDOR_SKU_CATALOG_TEST_URL||'http://127.0.0.1:3020';const b=await chromium.launch({headless:true}),p=await b.newPage();try{await p.goto(base);const c=(await p.locator('.font-mono').textContent()).trim();await p.getByPlaceholder('Username').fill('vendor-sku-e2e');await p.getByPlaceholder('Password').fill('local');await p.getByPlaceholder('Enter captcha').fill(c);await p.getByRole('button',{name:'Login'}).click();await p.waitForURL('**/app/dashboard');await p.goto(base+'/app/m/vendor-sku-catalog');await p.getByRole('button',{name:'Save'}).click();await p.getByText('Please fill all mandatory fields').waitFor();console.log('Vendor SKU Catalog browser workflow passed')}catch(e){console.error(e);process.exitCode=1}finally{await b.close()}
