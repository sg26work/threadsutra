import assert from'node:assert/strict';import{chromium}from'playwright';const base=process.env.ERETAIL_BASE_URL||'http://127.0.0.1:3002',send=async body=>{const r=await fetch(`${base}/api/inbound-enquiry`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});return{r,j:await r.json()}};const meta=await(await fetch(`${base}/api/inbound-enquiry`)).json();assert.deepEqual(meta.pageSizes,[20,50,100,200]);assert.deepEqual(meta.statuses.map(x=>x[1]),['Pending Confirmation','Pending Hold','GRN In Process','Confirmed','Cancelled']);assert.deepEqual(meta.inboundTypes.map(x=>x[1]),['With PO','Without PO','Delivered Return','Non Delivered Return','With STO','With QC','Against ASN'].sort((a,b)=>0));let x=await send({action:'search',REQ_SEARCH_FLAG:true,rows:20,page:1,sidx:'inbound0_.crtDate',sord:'desc',fromGRNNo:'GRN50001201'});assert.equal(x.j.records,1);const row=x.j.rows[0];for(const rows of[20,50,100,200]){x=await send({action:'search',REQ_SEARCH_FLAG:true,rows,page:1,sidx:'inbound0_.crtDate',sord:'desc',fromInboundNo:row.inboundNo});for(const k of['inboundList','gridModel','rows','page','total','records'])assert.ok(k in x.j)}x=await send({action:'search',REQ_SEARCH_FLAG:true,rows:20,page:1,ExtCustReturnNo:'RET1',inboundType:'1'});assert.match(x.j.error,/inbound type should be either/);x=await send({action:'qc'});assert.equal(x.j.error,'Please select a row.');x=await send({action:'qc',id:row.id});assert.equal(x.j.jsonMessage,'Inbound opened for QC');x=await send({action:'putaway',id:row.id});assert.equal(x.j.jsonMessage,'PutAway generated successfully');x=await send({action:'putaway',id:row.id});assert.equal(x.j.jsonMessage,'Existing PutAway opened');
const b=await chromium.launch({headless:true}),p=await b.newPage({viewport:{width:1440,height:1000}}),errors=[];p.on('pageerror',e=>errors.push(e.message));p.on('console',m=>m.type()==='error'&&errors.push(m.text()));try{await p.goto(base);const cap=(await p.locator('.font-mono').textContent()).trim();await p.getByPlaceholder('Username').fill('inbound');await p.getByPlaceholder('Password').fill('local');await p.getByPlaceholder('Enter captcha').fill(cap);await p.getByRole('button',{name:'Login'}).click();await p.waitForURL('**/app/dashboard');await p.goto(`${base}/app/r/inbound-enquiry`);for(const a of['Search','Reset','Advance Search','Add New','QC','PutAway'])await p.getByRole('button',{name:a,exact:true}).first().waitFor();for(const h of['Inbound No','Ext Inbound No','STO No','ASN No','PO No','GRN No','Creation Date','Inbound Type','Invoice No','vendor','Exp Qty','Receieved Qty','Status','QC Status','Inbound Location','Ext Return No','Ext Invoice No'])await p.getByRole('columnheader',{name:h,exact:true}).first().waitFor();for(const size of['20','50','100','200'])await p.getByLabel('Records per Page').selectOption(size);await p.getByRole('button',{name:'QC',exact:true}).click();await p.getByText('Please select a row.').waitFor();await p.getByRole('button',{name:'Advance Search',exact:true}).click();for(const label of['Inbound No','GRN No','Vendor','PO No','Invoice No','SKU Code','Order No','Lot No','USN No','LPN No','Ext Return No','Ext Invoice No','Advanced Status','Advanced Inbound Type'])await p.getByLabel(label,{exact:true}).waitFor();await p.getByRole('button',{name:'Close',exact:true}).click();await p.getByRole('button',{name:'Reset',exact:true}).click();assert.equal(await p.locator('vite-error-overlay').count(),0);assert.deepEqual(errors.filter(e=>!e.includes('status of 400')),[]);console.log('PASS Inbound Enquiry: live search model, primary/advanced filters, row-action validations, QC/PutAway lifecycle, paging, reset, and clean browser state.')}finally{await b.close()}
